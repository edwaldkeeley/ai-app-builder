@Fix all 6 code review bugs from ai_service.py refactor + increase timeouts

Code review fixes:
- Critical: guard f["path"] with .get() in json_parser.py
- Medium: fix_react_imports handles indented patterns properly
- Medium: React.X replacement skips string literals
- Medium: removed dead try_parse_json; _repair_json preserves apostrophes
- Medium: SSE parser works on copy, not accumulated_content
- Low: removed duplicate figma_service.py in CLAUDE.md

Timeout increases (2x):
- Backend TIMEOUT_SECONDS: 600 -> 1200 (20 min)
- Frontend API: 120s -> 600s
- Frontend WS: 600s -> 1200s
- Frontend Figma: 300s -> 600s
